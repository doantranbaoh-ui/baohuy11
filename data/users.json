def load_users():
    if not os.path.exists(USER_DATA):
        return {}
    with open(USER_DATA, "r", encoding="utf-8") as f:
        try:
            return json.load(f)
        except json.JSONDecodeError:
            return {}  # Nếu file trống hoặc lỗi JSON, trả về dict rỗng
